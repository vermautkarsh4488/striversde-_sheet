#include <bits/stdc++.h> 
void sortStack(stack<int> &s)
{
	// Write your code here
	 multiset<int> mp;
    while(!s.empty())
    {
        mp.insert(s.top());
        s.pop();
        
    }
    for(auto it : mp)
    {
        s.push(it);
        
    }

}
